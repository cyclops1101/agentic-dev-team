{
  "name": "@dev-team/mcp-server",
  "version": "1.0.0",
  "description": "MCP server for the agentic dev team skill — handles installation, dependency validation, status tracking, and run history",
  "type": "module",
  "bin": {
    "dev-team-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "dev": "tsx watch src/index.ts",
    "start": "node build/index.js"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "tsx": "^4.19.4",
    "@types/node": "^22.15.21"
  }
}
